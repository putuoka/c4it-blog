name: Merge Schedule
on:
  pull_request:
    types:
      - opened
      - edited
  schedule:
    - cron: 0,5,10,15,30,45,50,55 * * * *

jobs:
  merge_schedule:
    runs-on: ubuntu-latest
    steps:
      - uses: gr2m/merge-schedule-action@v1
        with: 
          merge_method: squash 
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
